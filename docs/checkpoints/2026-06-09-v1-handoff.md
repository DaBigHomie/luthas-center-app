# Luthas Center — v1 Migration Handoff (2026-06-09)

Branch: **`feat/wp-to-next-v1`** · Plan: `~/.claude/plans/review-the-current-deployment-scalable-orbit.md`

## State: v1 public site COMPLETE locally on real content. One external gate remains.

`npm run dev` → http://localhost:3000 renders the real migrated site (no Supabase needed —
JSON-fallback adapter). `npm run check` = typecheck + lint + build + **runtime smoke** (all green:
tsc 0, lint 0, build clean, smoke 514/514 prod).

## Commits (this branch)
- `2238673` migration foundation (extract → schema+seed → data layer → primitives/shell → 7 routes)
- `96dcafb` parity + SEO (title-slugs, sanitize, metadata/sitemap/robots/JSON-LD; redirects dormant)
- `753f004` runtime smoke-test gate + fixes (next/image fill+width, term-loader 500, media fallback)
- `efec129` reusable wp-content transform pipeline + curation + 296 branded covers

## Done
- **Content**: `data/extracted/` (47 courses, 57 lessons, 668 posts, 14 pages, 3 donation forms, taxonomy, 686 redirects, media manifest). Demo/PII discarded. TRACKED in git (DB is gone — Docker corrupted — so this is the only copy).
- **Schema**: `supabase/migrations/` (8) + `supabase/seed.sql` (18MB) — validated, NOT yet applied to a cloud project.
- **Media**: 2,932 webp in `.wp-source/_processed` (gitignored), served via `public/media` symlink; `scripts/image-processing` (in damieus-workflow-agents) + `scripts/.../upload-to-storage.ts` ready. 296 branded SVG covers in `public/covers` for offloaded images.
- **Frontend**: FSD, handbuilt primitives (`src/shared/ui`), shell, 7 routes (home, courses+detail+lesson, blog+post, about, contact, donate, give). Tokens from jnews theme (Tailwind 4 `@theme`).
- **Reusable pipeline**: `src/shared/lib/wp-content/` (Gutenberg/shortcode/link/img plugins) used at render + seed.
- **SEO**: per-route metadata, sitemap, robots, JSON-LD. **Redirects dormant** (`ENABLE_WP_REDIRECTS=1` to enable; `src/middleware.ts` — rename to `proxy.ts` per Next 16 advisory).

## THE GATE → connect Supabase (P5)
User is creating the project. On arrival: set `NEXT_PUBLIC_SUPABASE_URL` + anon key (service_role in `.env.local`), then `supabase link` → `db push` → seed → run upload-to-storage → flip env. App switches from JSON to cloud with zero code change. Then deploy to **`luthascenter.damieus.app`** (Vercel; CI = MALFIG, NO GitHub Actions).

## Pending / next (all unblocked except deploy)
- **Plugin remix**: drop `js_composer` (WPBakery) + `jnews` theme into `.wp-source/` → layer accurate shortcode rules into `wp-content/plugins/unwrap-shortcodes.ts` config (jnews_* are likely dynamic widgets → strip; standard vc_* already mapped). Shortcode inventory: vc_*~215, jnews_*~28, embed 240, ads/useyourdrive drop, woo/give→native.
- a11y/error-UX pass (axe; not-found/error/loading.tsx) · Playwright E2E · perf (ISR for 446-post blog)
- auth/admin (deferred D1; needs Supabase) · store + community (deferred D1)
- **#10 buy luthascenter.org/.com** (both unregistered) → Cloudflare → custom domain → enable redirects

## Key facts / gotchas
- Docker is CORRUPTED (containerd I/O error) — local Supabase stack unusable; go cloud. luthas-wp MySQL container is dead (content already extracted, safe).
- macOS blocks this process from `~/Downloads`; WP source was moved to `.wp-source/` (gitignored). Provide new files there, not Downloads.
- Course/lesson slugs are derived from titles (LearnDash had numeric/empty slugs); posts/pages keep real WP slugs.
- 420 images were Google-Drive-offloaded (not in backup) → branded covers + placeholders; backfill needs the live domain (dead).
- Process: Opus orchestrates; Sonnet/Haiku subagents + Workflow fan-outs; agents assigned via the `multi-model-task-assignment` skill (PRIME pre-launch → CORTEX writes deferred). MALFIG verdict: PASS.
